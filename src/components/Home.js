import React from 'react';
import {BrowserRouter, Link} from "react-router-dom";

const Home = (props) => {
    console.log(props);
    return (
        <div>
            <h1>Home</h1>

        </div>
    );
};

export default Home;
